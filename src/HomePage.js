import React from "react";

const HomePage = () => {
  return (
    <div>
      <h1>Sup</h1>
      <p>
        This is a simple Todo app where you can add tasks, mark them as completed
        and edit or delete them as needed.
      </p>
      <p>
        To get started, click on the Tasks link in the navigation bar to see a
        list of all your tasks.
      </p>
    </div>
  );
};

export default HomePage;
